# Text Reader: All Issues to Fix

## Already Fixed
1. ✅ Padding reduced (textSentenceInsetPx + CSS padding)
2. ✅ Footnotes (isInlineSpineChapterLink excludes footnote links)
3. ✅ Cover image (page-break-inside: avoid)
4. ✅ Vibration removed (performHapticFeedback → false)
5. ✅ Padding when toolbars hidden (stableTopChromeReservePx)

## Remaining Issues

### 1. Archive as Folder
**Problem**: Text formats inside archives (ZIP/RAR) take too long to load (black screen + loading wheel).
**Root cause**: `extractZipEntries` extracts ALL entries, not just the target.
**Fix**: Extract only the selected entry, not all entries.

### 2. Explain Settings
**Problem**: User wants settings to choose between online explanation and local dictionary.
**Fix**: Add settings UI for explain provider selection.

### 3. Tencent Model
**Problem**: User wants Tencent model from huggingface.com for translation.
**Fix**: Add new translation engine implementation.

### 4. TOC Navigation
**Problem**: In some formats, TOC chapters are not clickable in HTML content.
**Fix**: Need deeper investigation of JS tap handler.

### 5. Page Count + Progress
**Problem**: Incorrect page count and progress showing 100% too early.
**Fix**: Need investigation of progress calculation.
