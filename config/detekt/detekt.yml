# Detekt config — project-wide (2026-08-04)
# Only critical rules active. All noisy rules disabled via buildUponDefaultConfig=false.

build:
  maxIssues: 0

potential-bugs:
  UnsafeCallOnNullableType:
    active: true
  UnnecessaryNotNullCheck:
    active: true

empty-blocks:
  EmptyFunctionBlock:
    active: true
  EmptyWhenBlock:
    active: true
